---
description: Turn a repeated life task into a tested local workflow
argument-hint: "<task to improve>"
---
Help me build a reusable workflow for: $@
If no task was supplied, ask which repeated task I want to improve. Discover
existing specialist skills and tools before building anything. Propose the
smallest useful script, skill, or app; use the official q5m tools or CLI instead
of a new API/auth client. Separate reads from writes and provide an explicit
preview/approval step for consequential actions. Start with synthetic fixtures,
add focused tests, and keep credentials and real personal data out of source.
Use private/ for personal workflows and artifacts. Don't install dependencies,
publish, schedule, deploy, or perform live mutations without authorization.
